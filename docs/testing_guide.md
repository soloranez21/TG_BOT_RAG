# 🧪 Инструкция по тестированию персонального бота

## Шаг 1: Перезапустите Master Bot

Остановите текущий процесс (Ctrl+C) и запустите заново:

```powershell
python -m src.master_bot.main
```

## Шаг 2: Проверьте логи при запуске

Вы должны увидеть:

```
[SPAWN] Starting spawn process for user {id}
[SPAWN] Personal bot path: c:\Users\IgorTheCookie\Desktop\TG_BOT_RAG\src\personal_bot\main.py
[SPAWN] Command: python.exe c:\Users\...\main.py --user-id ...
[SPAWN] Python executable: ...
[SPAWN] ✅ Spawned personal bot for user {id} (PID: {pid})
[SPAWN] Process is alive and running
```

## Шаг 3: Используйте команды в Telegram

### Если у вас уже есть бот:

1. Отправьте `/status` - проверьте статус
2. Отправьте `/debug` - посмотрите детальную информацию о процессе
3. Если бот не работает, отправьте `/restart`

### Если бота нет:

1. Отправьте `/start`
2. Следуйте инструкциям
3. После создания проверьте `/debug`

## Шаг 4: Анализ проблем

### Если процесс умирает сразу:

1. Проверьте вывод `/debug`
2. Посмотрите STDERR в логах
3. Типичные ошибки:
   - `ModuleNotFoundError` - не установлены зависимости
   - `Unauthorized` - неверный токен бота
   - `Connection refused` - Qdrant не запущен
   - `Invalid API key` - неверный OpenAI ключ

### Если процесс запускается, но не отвечает:

1. Проверьте PID в диспетчере задач
2. Попробуйте написать боту напрямую в Telegram
3. Проверьте логи master bot

## Шаг 5: Проверка вручную

Вы можете запустить персональный бот вручную для тестирования:

```powershell
python src\personal_bot\main.py `
    --user-id 123456789 `
    --bot-token "YOUR_BOT_TOKEN" `
    --openai-key "YOUR_OPENAI_KEY" `
    --collection "user_123456789" `
    --qdrant-url "http://localhost:6333"
```

Замените значения на реальные из вашей БД.

## 📋 Чеклист проверки

- [ ] Master bot запускается без ошибок
- [ ] При создании бота появляются логи `[SPAWN]`
- [ ] PID процесса отображается в логах
- [ ] Процесс остается живым (не умирает сразу)
- [ ] `/status` показывает "🟢 Работает"
- [ ] `/debug` показывает status: "running"
- [ ] Персональный бот отвечает в Telegram
